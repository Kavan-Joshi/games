# Core dependencies
openenv-core[core]>=0.2.2
fastapi>=0.115.0
uvicorn>=0.24.0
pydantic>=2.0.0

# Demo UI
gradio>=4.0.0

# Training (optional - install separately for GPU training)
# pip install "unsloth[colab-new]@git+https://github.com/unslothai/unsloth.git" trl datasets transformers
# pip install wandb  # optional: for wandb logging
